//! Outcome domain type (placeholder until Milestone E).

/// Verification outcome (`satisfied`, `not_satisfied`, `indeterminate`).
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct Outcome;
