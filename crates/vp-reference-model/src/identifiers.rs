//! Stable identifiers for conformance comparison (placeholder).

/// Domain identifiers bound to a claim or evaluation session.
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct Identifiers;
